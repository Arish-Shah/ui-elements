{
  "name": "static-site-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf build && mkdir build",
    "build": "node src/app.js",
    "postbuild": "npm run copy:static && npm run copy:assets",
    "copy:static": "cp -ar static/. build",
    "copy:assets": "cp -ar src/assets build",
    "start": "serve build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gray-matter": "^4.0.2",
    "remark": "^13.0.0",
    "remark-html": "^13.0.1",
    "serve": "^11.3.2"
  }
}
